# C／C++ 共同基礎 100 題：學生版

> 作答方式：每一題可自行選擇 **C** 或 **C++**。除題目明確要求外，不使用條件判斷、迴圈、陣列或函式拆分。

範圍：開發環境、第一個程式、基本輸出、變數、輸入輸出、基本資料型態、型別轉換、算術／比較／邏輯運算子與簡單表示式。

---

## Part A：開發環境與基本概念

### 1. 原始碼副檔名

選擇 C 或 C++ 作答：寫出該語言原始碼常用的副檔名。

請回答：

```text

```

---

### 2. 編譯指令

假設原始檔名為 `main.c` 或 `main.cpp`，請寫出把它編譯成 `main` 執行檔的指令。

請回答：

```text

```

---

### 3. 執行程式

在 macOS 或 Linux 的終端機中，如何執行目前資料夾內名為 `main` 的程式？

請回答：

```text

```

---

### 4. 開發流程排序

請將下列步驟排成合理順序：執行、測試結果、撰寫原始碼、編譯。

請回答：

```text

```

---

### 5. 編譯器的工作

請用一句話說明編譯器的主要工作。

請回答：

```text

```

---

### 6. 語法錯誤

程式少了一個分號，導致編譯失敗。這屬於哪一類錯誤？

請回答：

```text

```

---

### 7. 執行期錯誤

程式成功編譯，但執行時因非法操作而終止。這屬於哪一類錯誤？

請回答：

```text

```

---

### 8. 邏輯錯誤

程式可以執行，但把長方形面積寫成長加寬。這屬於哪一類錯誤？

請回答：

```text

```

---

### 9. IDE 的用途

請列出 IDE 通常整合的兩種以上功能。

請回答：

```text

```

---

### 10. 成功編譯的意義

程式成功編譯是否代表答案一定正確？請簡短說明。

請回答：

```text

```

---

## Part B：第一個程式與基本輸出

### 11. Hello, World!

撰寫完整程式，輸出 `Hello, World!`。

**輸入：** 無。

**輸出：** 輸出指定文字並換行。

**範例輸入：** 無

**範例輸出：**

```text
Hello, World!
```

請寫程式：

```c

```

---

### 12. 輸出兩行文字

輸出兩行：第一行是 `C and C++`，第二行是 `Week 1`。

**輸入：** 無。

**輸出：** 依指定格式輸出兩行。

**範例輸入：** 無

**範例輸出：**

```text
C and C++
Week 1
```

請寫程式：

```c

```

---

### 13. 輸出雙引號

輸出 `She said, "Hello!"`。

**輸入：** 無。

**輸出：** 輸出包含雙引號的句子。

**範例輸入：** 無

**範例輸出：**

```text
She said, "Hello!"
```

請寫程式：

```c

```

---

### 14. 輸出反斜線

輸出路徑 `C:\code\main`。

**輸入：** 無。

**輸出：** 輸出指定路徑。

**範例輸入：** 無

**範例輸出：**

```text
C:\code\main
```

請寫程式：

```c

```

---

### 15. Tab 分隔

使用 Tab 將 `Name` 與 `Score` 分隔後輸出。

**輸入：** 無。

**輸出：** 輸出兩個欄位，中間使用 Tab。

**範例輸入：** 無

**範例輸出：**

```text
Name	Score
```

請寫程式：

```c

```

---

### 16. 三個字母

輸出 `A B C`，字母之間各有一個空格。

**輸入：** 無。

**輸出：** 輸出指定文字。

**範例輸入：** 無

**範例輸出：**

```text
A B C
```

請寫程式：

```c

```

---

### 17. 算式文字

輸出 `3 + 5 = 8`。

**輸入：** 無。

**輸出：** 輸出指定算式。

**範例輸入：** 無

**範例輸出：**

```text
3 + 5 = 8
```

請寫程式：

```c

```

---

### 18. 基本資料卡

輸出兩行：`Name: Amy` 與 `Age: 18`。

**輸入：** 無。

**輸出：** 依指定格式輸出。

**範例輸入：** 無

**範例輸出：**

```text
Name: Amy
Age: 18
```

請寫程式：

```c

```

---

### 19. 文字方框

輸出下列三行方框：`*****`、`* C *`、`*****`。

**輸入：** 無。

**輸出：** 依指定格式輸出。

**範例輸入：** 無

**範例輸出：**

```text
*****
* C *
*****
```

請寫程式：

```c

```

---

### 20. 最小可執行程式

撰寫一個完整程式，程式不需輸出任何內容，但必須正常結束。

**輸入：** 無。

**輸出：** 無。

**範例輸入：** 無

**範例輸出：**

```text
（無輸出）
```

請寫程式：

```c

```

---

## Part C：變數與固定資料計算

### 21. 輸出整數變數

宣告整數變數 `age`，初始值為 18，然後輸出它。

**輸入：** 無。

**輸出：** 輸出 `18`。

**範例輸入：** 無

**範例輸出：**

```text
18
```

請寫程式：

```c

```

---

### 22. 輸出浮點變數

宣告浮點變數 `price`，初始值為 19.5，然後輸出到小數點後一位。

**輸入：** 無。

**輸出：** 輸出 `19.5`。

**範例輸入：** 無

**範例輸出：**

```text
19.5
```

請寫程式：

```c

```

---

### 23. 輸出字元變數

宣告字元變數 `grade`，初始值為 `A`，然後輸出它。

**輸入：** 無。

**輸出：** 輸出 `A`。

**範例輸入：** 無

**範例輸出：**

```text
A
```

請寫程式：

```c

```

---

### 24. 兩變數相加

宣告 `a = 5`、`b = 7`，計算並輸出兩者總和。

**輸入：** 無。

**輸出：** 輸出 `12`。

**範例輸入：** 無

**範例輸出：**

```text
12
```

請寫程式：

```c

```

---

### 25. 重新指定變數

令 `x` 的初始值為 3，再把 `x` 改成 8，最後輸出 `x`。

**輸入：** 無。

**輸出：** 輸出 `8`。

**範例輸入：** 無

**範例輸出：**

```text
8
```

請寫程式：

```c

```

---

### 26. 交換兩個變數

令 `a = 10`、`b = 20`，使用第三個變數交換兩者，再輸出 `a b`。

**輸入：** 無。

**輸出：** 輸出 `20 10`。

**範例輸入：** 無

**範例輸出：**

```text
20 10
```

請寫程式：

```c

```

---

### 27. 固定長方形面積

長為 8、寬為 5，使用變數計算並輸出長方形面積。

**輸入：** 無。

**輸出：** 輸出 `40`。

**範例輸入：** 無

**範例輸出：**

```text
40
```

請寫程式：

```c

```

---

### 28. 固定圓面積

使用 `pi = 3.14`、半徑 5，計算圓面積並輸出到小數點後一位。

**輸入：** 無。

**輸出：** 輸出 `78.5`。

**範例輸入：** 無

**範例輸出：**

```text
78.5
```

請寫程式：

```c

```

---

### 29. 固定折扣

商品原價為 100 元，折扣金額為 15 元，計算並輸出折扣後價格。

**輸入：** 無。

**輸出：** 輸出 `85`。

**範例輸入：** 無

**範例輸出：**

```text
85
```

請寫程式：

```c

```

---

### 30. 秒數拆解

總秒數為 3671，換算成小時、分鐘、秒，並依序輸出。

**輸入：** 無。

**輸出：** 輸出 `1 1 11`。

**範例輸入：** 無

**範例輸出：**

```text
1 1 11
```

請寫程式：

```c

```

---

## Part D：輸入與整數計算

### 31. 讀取並輸出整數

讀取一個整數，原樣輸出。

**輸入：** 一個整數。

**輸出：** 輸出相同整數。

**範例輸入：**

```text
42
```

**範例輸出：**

```text
42
```

請寫程式：

```c

```

---

### 32. 兩整數總和

讀取兩個整數，輸出它們的總和。

**輸入：** 兩個整數 `a b`。

**輸出：** 輸出 `a + b`。

**範例輸入：**

```text
8 13
```

**範例輸出：**

```text
21
```

請寫程式：

```c

```

---

### 33. 兩整數差

讀取兩個整數 `a b`，輸出 `a - b`。

**輸入：** 兩個整數。

**輸出：** 輸出差。

**範例輸入：**

```text
20 7
```

**範例輸出：**

```text
13
```

請寫程式：

```c

```

---

### 34. 兩整數乘積

讀取兩個整數，輸出它們的乘積。

**輸入：** 兩個整數。

**輸出：** 輸出乘積。

**範例輸入：**

```text
6 9
```

**範例輸出：**

```text
54
```

請寫程式：

```c

```

---

### 35. 整數商

讀取兩個整數 `a b`，輸出整數除法 `a / b` 的商。保證 `b` 不為 0。

**輸入：** 兩個整數。

**輸出：** 輸出整數商。

**範例輸入：**

```text
17 5
```

**範例輸出：**

```text
3
```

請寫程式：

```c

```

---

### 36. 整數餘數

讀取兩個整數 `a b`，輸出 `a % b`。保證 `b` 不為 0。

**輸入：** 兩個整數。

**輸出：** 輸出餘數。

**範例輸入：**

```text
17 5
```

**範例輸出：**

```text
2
```

請寫程式：

```c

```

---

### 37. 三整數總和

讀取三個整數，輸出總和。

**輸入：** 三個整數。

**輸出：** 輸出總和。

**範例輸入：**

```text
3 7 11
```

**範例輸出：**

```text
21
```

請寫程式：

```c

```

---

### 38. 三整數的整數平均

讀取三個整數，使用整數除法輸出它們的平均值。

**輸入：** 三個整數。

**輸出：** 輸出 `(a+b+c)/3` 的整數結果。

**範例輸入：**

```text
5 8 11
```

**範例輸出：**

```text
8
```

請寫程式：

```c

```

---

### 39. 長方形面積

讀取長與寬，輸出長方形面積。

**輸入：** 兩個整數：長、寬。

**輸出：** 輸出面積。

**範例輸入：**

```text
12 4
```

**範例輸出：**

```text
48
```

請寫程式：

```c

```

---

### 40. 長方形周長

讀取長與寬，輸出長方形周長。

**輸入：** 兩個整數：長、寬。

**輸出：** 輸出周長。

**範例輸入：**

```text
12 4
```

**範例輸出：**

```text
32
```

請寫程式：

```c

```

---

### 41. 正方形面積與周長

讀取正方形邊長，依序輸出面積與周長，中間一個空格。

**輸入：** 一個整數邊長。

**輸出：** 輸出 `面積 周長`。

**範例輸入：**

```text
6
```

**範例輸出：**

```text
36 24
```

請寫程式：

```c

```

---

### 42. 分鐘換算

讀取總分鐘數，換算成小時與剩餘分鐘。

**輸入：** 一個非負整數。

**輸出：** 輸出 `小時 分鐘`。

**範例輸入：**

```text
135
```

**範例輸出：**

```text
2 15
```

請寫程式：

```c

```

---

### 43. 秒數換算

讀取總秒數，換算成小時、分鐘、秒。

**輸入：** 一個非負整數。

**輸出：** 輸出 `小時 分鐘 秒`。

**範例輸入：**

```text
7325
```

**範例輸出：**

```text
2 2 5
```

請寫程式：

```c

```

---

### 44. 硬幣總額

依序讀取 1 元、5 元、10 元硬幣的數量，輸出總金額。

**輸入：** 三個非負整數。

**輸出：** 輸出硬幣總額。

**範例輸入：**

```text
3 4 2
```

**範例輸出：**

```text
43
```

請寫程式：

```c

```

---

### 45. 公里換公尺

讀取整數公里數，換算並輸出公尺數。

**輸入：** 一個整數公里數。

**輸出：** 輸出公尺數。

**範例輸入：**

```text
7
```

**範例輸出：**

```text
7000
```

請寫程式：

```c

```

---

### 46. 公斤與公克

讀取公斤與公克，換算成總公克數。

**輸入：** 兩個整數：公斤、公克。

**輸出：** 輸出總公克。

**範例輸入：**

```text
3 250
```

**範例輸出：**

```text
3250
```

請寫程式：

```c

```

---

### 47. 讀取字元

讀取一個非空白字元並原樣輸出。

**輸入：** 一個字元。

**輸出：** 輸出相同字元。

**範例輸入：**

```text
K
```

**範例輸出：**

```text
K
```

請寫程式：

```c

```

---

### 48. 反向輸出兩字元

讀取兩個非空白字元，反向輸出，中間一個空格。

**輸入：** 兩個字元。

**輸出：** 輸出第二個字元再輸出第一個。

**範例輸入：**

```text
A B
```

**範例輸出：**

```text
B A
```

請寫程式：

```c

```

---

### 49. 前一個與下一個整數

讀取整數 `n`，輸出 `n-1`、`n`、`n+1`。

**輸入：** 一個整數。

**輸出：** 輸出三個整數。

**範例輸入：**

```text
10
```

**範例輸出：**

```text
9 10 11
```

請寫程式：

```c

```

---

### 50. 拆解三位數

讀取一個三位正整數，依序輸出百位、十位、個位。

**輸入：** 一個三位正整數。

**輸出：** 輸出三個位數。

**範例輸入：**

```text
572
```

**範例輸出：**

```text
5 7 2
```

請寫程式：

```c

```

---

## Part E：資料型態與型別轉換

### 51. 年齡的資料型態

儲存一般人的年齡時，`int`、`double`、`char` 中哪一個最合適？

請回答：

```text

```

---

### 52. 價格的資料型態

價格可能包含小數時，`int`、`double`、`char` 中哪一個較合適？

請回答：

```text

```

---

### 53. 單一字母的資料型態

儲存單一英文字母時，`int`、`double`、`char` 中哪一個較合適？

請回答：

```text

```

---

### 54. 單引號與雙引號

單一字元與字串文字通常分別使用哪一種引號？

請回答：

```text

```

---

### 55. const 的作用

在 C 或 C++ 中，宣告變數時加入 `const` 通常表示什麼？

請回答：

```text

```

---

### 56. sizeof 的作用

`sizeof` 運算子主要用來取得什麼資訊？

請回答：

```text

```

---

### 57. 整數除法

計算並輸出整數運算 `7 / 2` 的結果。

**輸入：** 無。

**輸出：** 輸出整數除法結果。

**範例輸入：** 無

**範例輸出：**

```text
3
```

請寫程式：

```c

```

---

### 58. 浮點除法

計算並輸出 `7.0 / 2.0`，結果顯示到小數點後一位。

**輸入：** 無。

**輸出：** 輸出 `3.5`。

**範例輸入：** 無

**範例輸出：**

```text
3.5
```

請寫程式：

```c

```

---

### 59. 轉型後相除

令 `a = 7`、`b = 2`，先把其中一個值轉成浮點型別，再相除並輸出到小數點後一位。

**輸入：** 無。

**輸出：** 輸出 `3.5`。

**範例輸入：** 無

**範例輸出：**

```text
3.5
```

請寫程式：

```c

```

---

### 60. 浮點轉整數

把浮點數 `8.9` 明確轉成整數並輸出結果。

**輸入：** 無。

**輸出：** 輸出 `8`。

**範例輸入：** 無

**範例輸出：**

```text
8
```

請寫程式：

```c

```

---

### 61. 精確平均值

讀取兩個整數，輸出浮點平均值到小數點後一位。

**輸入：** 兩個整數。

**輸出：** 輸出平均值。

**範例輸入：**

```text
5 8
```

**範例輸出：**

```text
6.5
```

請寫程式：

```c

```

---

### 62. 公分換公尺

讀取公分數（可含小數），換算成公尺並輸出到小數點後兩位。

**輸入：** 一個浮點數。

**輸出：** 輸出公尺數。

**範例輸入：**

```text
250
```

**範例輸出：**

```text
2.50
```

請寫程式：

```c

```

---

### 63. 攝氏轉華氏

讀取攝氏溫度，使用 `F = C × 9 / 5 + 32` 換算，輸出到小數點後一位。

**輸入：** 一個浮點數。

**輸出：** 輸出華氏溫度。

**範例輸入：**

```text
25
```

**範例輸出：**

```text
77.0
```

請寫程式：

```c

```

---

### 64. 華氏轉攝氏

讀取華氏溫度，使用 `C = (F - 32) × 5 / 9` 換算，輸出到小數點後一位。

**輸入：** 一個浮點數。

**輸出：** 輸出攝氏溫度。

**範例輸入：**

```text
68
```

**範例輸出：**

```text
20.0
```

請寫程式：

```c

```

---

### 65. 圓面積

讀取半徑，使用常數 `3.14` 計算圓面積，輸出到小數點後兩位。

**輸入：** 一個浮點數半徑。

**輸出：** 輸出圓面積。

**範例輸入：**

```text
4
```

**範例輸出：**

```text
50.24
```

請寫程式：

```c

```

---

### 66. 單利計算

讀取本金、年利率百分比、年數，計算單利 `本金 × 利率 / 100 × 年數`，輸出到小數點後兩位。

**輸入：** 三個浮點數。

**輸出：** 輸出利息。

**範例輸入：**

```text
10000 2.5 3
```

**範例輸出：**

```text
750.00
```

請寫程式：

```c

```

---

### 67. 含稅價格

讀取未稅價格與稅率百分比，計算含稅價格，輸出到小數點後兩位。

**輸入：** 兩個浮點數。

**輸出：** 輸出含稅價格。

**範例輸入：**

```text
200 5
```

**範例輸出：**

```text
210.00
```

請寫程式：

```c

```

---

### 68. 百分比

讀取答對題數與總題數，輸出答對百分比到小數點後兩位。保證總題數不為 0。

**輸入：** 兩個整數。

**輸出：** 輸出百分比，不加 `%` 符號。

**範例輸入：**

```text
43 50
```

**範例輸出：**

```text
86.00
```

請寫程式：

```c

```

---

### 69. BMI

讀取體重（公斤）與身高（公尺），計算 `BMI = 體重 / 身高²`，輸出到小數點後兩位。

**輸入：** 兩個浮點數。

**輸出：** 輸出 BMI。

**範例輸入：**

```text
60 1.7
```

**範例輸出：**

```text
20.76
```

請寫程式：

```c

```

---

### 70. 加權成績

讀取平時成績與考試成績，平時占 40%、考試占 60%，輸出加權成績到小數點後一位。

**輸入：** 兩個浮點數。

**輸出：** 輸出加權成績。

**範例輸入：**

```text
80 90
```

**範例輸出：**

```text
86.0
```

請寫程式：

```c

```

---

## Part F：運算子與表示式

### 71. 乘法優先

計算並輸出 `2 + 3 * 4`。

**輸入：** 無。

**輸出：** 輸出結果。

**範例輸入：** 無

**範例輸出：**

```text
14
```

請寫程式：

```c

```

---

### 72. 括號優先

計算並輸出 `(2 + 3) * 4`。

**輸入：** 無。

**輸出：** 輸出結果。

**範例輸入：** 無

**範例輸出：**

```text
20
```

請寫程式：

```c

```

---

### 73. 同層級由左至右一

計算並輸出 `20 / 5 * 2`。

**輸入：** 無。

**輸出：** 輸出結果。

**範例輸入：** 無

**範例輸出：**

```text
8
```

請寫程式：

```c

```

---

### 74. 括號改變除法

計算並輸出 `20 / (5 * 2)`。

**輸入：** 無。

**輸出：** 輸出結果。

**範例輸入：** 無

**範例輸出：**

```text
2
```

請寫程式：

```c

```

---

### 75. 同層級由左至右二

計算並輸出 `10 - 3 - 2`。

**輸入：** 無。

**輸出：** 輸出結果。

**範例輸入：** 無

**範例輸出：**

```text
5
```

請寫程式：

```c

```

---

### 76. 連續整數除法

計算並輸出 `18 / 3 / 2`。

**輸入：** 無。

**輸出：** 輸出結果。

**範例輸入：** 無

**範例輸出：**

```text
3
```

請寫程式：

```c

```

---

### 77. 個位數

讀取非負整數 `n`，輸出個位數。

**輸入：** 一個非負整數。

**輸出：** 輸出 `n % 10`。

**範例輸入：**

```text
4837
```

**範例輸出：**

```text
7
```

請寫程式：

```c

```

---

### 78. 最後兩位

讀取非負整數 `n`，輸出最後兩位所形成的數值。

**輸入：** 一個非負整數。

**輸出：** 輸出 `n % 100`。

**範例輸入：**

```text
4837
```

**範例輸出：**

```text
37
```

請寫程式：

```c

```

---

### 79. 百位數

讀取至少三位的非負整數，輸出百位數。

**輸入：** 一個非負整數。

**輸出：** 輸出百位數。

**範例輸入：**

```text
4837
```

**範例輸出：**

```text
8
```

請寫程式：

```c

```

---

### 80. 奇偶餘數

讀取整數 `n`，輸出 `n % 2`。本題不使用條件判斷。

**輸入：** 一個非負整數。

**輸出：** 偶數輸出 0，奇數輸出 1。

**範例輸入：**

```text
27
```

**範例輸出：**

```text
1
```

請寫程式：

```c

```

---

### 81. 複合指定加法

令 `x = 10`，使用 `+=` 讓它增加 7，再輸出。

**輸入：** 無。

**輸出：** 輸出 `17`。

**範例輸入：** 無

**範例輸出：**

```text
17
```

請寫程式：

```c

```

---

### 82. 複合指定減法

令 `x = 10`，使用 `-=` 讓它減少 4，再輸出。

**輸入：** 無。

**輸出：** 輸出 `6`。

**範例輸入：** 無

**範例輸出：**

```text
6
```

請寫程式：

```c

```

---

### 83. 複合指定乘法

令 `x = 6`，使用 `*=` 讓它乘以 5，再輸出。

**輸入：** 無。

**輸出：** 輸出 `30`。

**範例輸入：** 無

**範例輸出：**

```text
30
```

請寫程式：

```c

```

---

### 84. 複合指定除法

令 `x = 40`，使用 `/=` 讓它除以 8，再輸出。

**輸入：** 無。

**輸出：** 輸出 `5`。

**範例輸入：** 無

**範例輸出：**

```text
5
```

請寫程式：

```c

```

---

### 85. 遞增運算

令 `x = 9`，使用遞增運算子讓 `x` 增加 1，再輸出。

**輸入：** 無。

**輸出：** 輸出 `10`。

**範例輸入：** 無

**範例輸出：**

```text
10
```

請寫程式：

```c

```

---

### 86. 大於比較

讀取兩個整數 `a b`，輸出比較式 `a > b` 的結果，以 0 或 1 表示。

**輸入：** 兩個整數。

**輸出：** 成立輸出 1，否則輸出 0。

**範例輸入：**

```text
9 4
```

**範例輸出：**

```text
1
```

請寫程式：

```c

```

---

### 87. 相等比較

讀取兩個整數 `a b`，輸出比較式 `a == b` 的結果，以 0 或 1 表示。

**輸入：** 兩個整數。

**輸出：** 成立輸出 1，否則輸出 0。

**範例輸入：**

```text
7 7
```

**範例輸出：**

```text
1
```

請寫程式：

```c

```

---

### 88. 邏輯且

讀取整數 `x`，輸出 `(x > 0) && (x < 10)` 的結果，以 0 或 1 表示。

**輸入：** 一個整數。

**輸出：** 兩個條件都成立時輸出 1。

**範例輸入：**

```text
6
```

**範例輸出：**

```text
1
```

請寫程式：

```c

```

---

### 89. 邏輯或

讀取整數 `x`，輸出 `(x < 0) || (x > 100)` 的結果，以 0 或 1 表示。

**輸入：** 一個整數。

**輸出：** 至少一個條件成立時輸出 1。

**範例輸入：**

```text
150
```

**範例輸出：**

```text
1
```

請寫程式：

```c

```

---

### 90. 邏輯非

讀取整數 `x`，輸出 `!(x == 0)` 的結果，以 0 或 1 表示。

**輸入：** 一個整數。

**輸出：** `x` 不等於 0 時輸出 1。

**範例輸入：**

```text
5
```

**範例輸出：**

```text
1
```

請寫程式：

```c

```

---

## Part G：綜合應用

### 91. 商品小計

讀取商品單價與數量，輸出小計。

**輸入：** 兩個整數：單價、數量。

**輸出：** 輸出單價乘以數量。

**範例輸入：**

```text
35 4
```

**範例輸出：**

```text
140
```

請寫程式：

```c

```

---

### 92. 平均分帳

讀取總金額與人數，計算每人平均金額，輸出到小數點後兩位。保證人數大於 0。

**輸入：** 一個浮點總金額與一個整數人數。

**輸出：** 輸出每人平均金額。

**範例輸入：**

```text
1000 6
```

**範例輸出：**

```text
166.67
```

請寫程式：

```c

```

---

### 93. 找零

讀取付款金額與商品價格，輸出找零。保證付款金額不少於商品價格。

**輸入：** 兩個整數：付款、價格。

**輸出：** 輸出找零。

**範例輸入：**

```text
500 368
```

**範例輸出：**

```text
132
```

請寫程式：

```c

```

---

### 94. 天數換週數

讀取總天數，換算成完整週數與剩餘天數。

**輸入：** 一個非負整數。

**輸出：** 輸出 `週數 剩餘天數`。

**範例輸入：**

```text
45
```

**範例輸出：**

```text
6 3
```

請寫程式：

```c

```

---

### 95. 三項加權成績

讀取作業、期中、期末成績，權重分別為 20%、30%、50%，輸出加權成績到小數點後一位。

**輸入：** 三個浮點數。

**輸出：** 輸出加權成績。

**範例輸入：**

```text
80 70 90
```

**範例輸出：**

```text
82.0
```

請寫程式：

```c

```

---

### 96. 座標中點

讀取兩點 `(x1, y1)`、`(x2, y2)`，輸出中點座標到小數點後一位。

**輸入：** 四個整數。

**輸出：** 輸出 `中點x 中點y`。

**範例輸入：**

```text
1 2 6 8
```

**範例輸出：**

```text
3.5 5.0
```

請寫程式：

```c

```

---

### 97. 兩點距離平方

讀取兩點整數座標，計算 `(x2-x1)^2 + (y2-y1)^2` 並輸出。不要開根號。

**輸入：** 四個整數。

**輸出：** 輸出距離平方。

**範例輸入：**

```text
1 2 4 6
```

**範例輸出：**

```text
25
```

請寫程式：

```c

```

---

### 98. 增加秒數

讀取目前的小時、分鐘、秒，以及要增加的秒數。換算後輸出 24 小時制的新時間。本題不使用條件判斷。

**輸入：** 四個非負整數 `h m s add`。

**輸出：** 輸出新的 `h m s`。

**範例輸入：**

```text
23 59 50 20
```

**範例輸出：**

```text
0 0 10
```

請寫程式：

```c

```

---

### 99. 四位數字和

讀取一個四位正整數，輸出四個位數的總和。

**輸入：** 一個四位正整數。

**輸出：** 輸出位數和。

**範例輸入：**

```text
5729
```

**範例輸出：**

```text
23
```

請寫程式：

```c

```

---

### 100. 折扣與稅金

讀取單價、數量、折扣百分比與稅率百分比。先算小計，再折扣，最後加稅，輸出最終金額到小數點後兩位。

**輸入：** 四個數值：單價、數量、折扣率、稅率。

**輸出：** 輸出最終金額。

**範例輸入：**

```text
100 3 10 5
```

**範例輸出：**

```text
283.50
```

請寫程式：

```c

```

---
